apiVersion: v1
kind: Service

metadata:
  name: main-gateway-service
  namespace: NAMESPACE

spec:
  selector:
    project: PROJECT

  ports:
    - name: grpc
      protocol: TCP
      port: 50050
      targetPort: 50050